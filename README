dotss!!!

sudo install:[fd-ripgrep-fzf]

sudo configure:[{
  bash-zsh,
  vim-nvim-tmux,
  kitty,
}]

NOTE:
A minimal dots, mostly defaults with few tweaks!

vim: 
  *build from source,
   for yank sync to system clipboard
  *install clanng for lsp
font: 
  *Ioskeva (install script in scripts/)
