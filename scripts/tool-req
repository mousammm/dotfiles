sudo pacman -S xorg-server xorg-xinit xf86-video-amdgup alsa-utils pipewire-pulse ttf-dejavu

sway
 ;; pulseaudio

Kitty
firefox, (ublock + darkreader) ;; about:config => (ui.key.menuAccessKeyFocuses)

tmux/vim/nvim
  ;; wl-clipboard
  ;; fd,ripgrep,fzf
  ;; feh for img

;; wget,curl,unzip,tar,stow,git

- openssh
- os-prober,fuse3
- man-db,man-pages,man-pages-posix
- sudo pacman -S vulkan-radeon
